{
  "name": "particularmarkdown",
  "displayName": "ParticularMarkdown",
  "description": "",
  "version": "0.0.1",
  "publisher": "ParticularSoftware",
  "engines": {
    "vscode": "^1.0.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:particularMarkdown.showMarkdown"
  ],
  "main": "./out/src/extension",
  "contributes": {
    "commands": [
      {
        "command": "particularMarkdown.showMarkdown",
        "title": "Show Particular Markdown"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "node ./node_modules/vscode/bin/compile",
    "compile": "node ./node_modules/vscode/bin/compile -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install"
  },
  "devDependencies": {
    "electron-rebuild": "^1.1.3",
    "typescript": "^1.8.5",
    "vscode": "^0.11.0"
  },
  "dependencies": {
    "edge": "^5.0.0"
  }
}
